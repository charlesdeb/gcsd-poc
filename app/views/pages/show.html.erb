<%# content_for :title, @page.title %>
<% description @page.meta_description %>
<article class="grid grid-cols-1 md:grid-cols-2 gap-8">
  <h1 class="md:row-start-1 md:col-span-2"><%= title @page.title %></h1>
  <div class="block md:row-start-2 md:col-start-2 md:row-span-4">
    <%= image_tag(@page.featured_image.variant(format: :webp), loading: :eager, class: "shadow-lg shadow-celery-300 w-full") if @page.featured_image.representable? %>
  </div>
  <section class="md:row-start-2 md:col-start-1">
    <%= @page.body %>
  </section>
  <div class="md:row-start-3 md:col-start-1 flex flex-wrap gap-2">
    <% if current_user && current_user.admin? %>
      <%= link_to 'Edit this page', edit_admin_page_path(id: @page), class: "rounded-lg py-3 px-5 bg-celery-200 inline-block font-medium" %>
      <div class="inline-block">
        <%= button_to 'Destroy this page', page_path(id: @page), method: :delete, class: "rounded-lg py-3 px-5 bg-celery-200 font-medium" %>
      </div>
      <%= link_to 'Back to pages', admin_pages_path, class: "rounded-lg py-3 px-5 bg-celery-200 inline-block font-medium" %>
    <% end %>
  </div>
</article>
